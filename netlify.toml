[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://smilecare-final.onrender.com"
  NODE_VERSION = "20"
# Avoid post-processing hangs
[build.processing]
  skip_processing = true
